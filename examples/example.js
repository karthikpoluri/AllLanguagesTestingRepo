// A free function
function greet(name) {
  console.log(\"Hello, \" + name);
}

// An arrow function
const add = (a, b) => a + b;

// A class with extends
class MyClass extends BaseClass {
  // A method
  sayHello() {
    console.log(\"Hi!\");
  }
}

interface MyInterface {
  id: number;
  name: string;
  doSomething(): void;
}
